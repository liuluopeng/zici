{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "dist",
                "installCommand": "echo 'Skipping automatic install'",
                "buildCommand": "bash -c 'curl https://sh.rustup.rs -sSf | sh -s -- -y && source $HOME/.cargo/env && rustup target add wasm32-unknown-unknown && cargo install wasm-pack && cd ../ && wasm-pack build -d www_vue/pkg --target web && cd www_vue && npm install --legacy-peer-deps && npm run cnchar-prod && npm run build'"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "$1"
        }
    ],
    "build": {
        "env": {
            "NODE_ENV": "production"
        }
    }
}